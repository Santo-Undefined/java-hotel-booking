spring:
  mongodb:
    uri: ${HOTEL_MONGO_URI:mongodb://localhost:27017/booking-test}
  swagger-ui:
    enabled: false

security:
  jwt:
    secret-key: xxxxxxxx
    expiration-time: 12345

server:
  port: 3000

kind: Application